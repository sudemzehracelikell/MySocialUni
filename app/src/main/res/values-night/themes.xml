<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MySocilUni" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Buton rengi -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorOnPrimary">@color/black</item>

        <!-- Buton arka plan rengi -->
        <item name="android:colorButtonNormal">#8e3d3e</item>  <!-- Butonların arka plan rengi -->

        <!-- Arka plan rengi -->
        <item name="android:windowBackground">#8e3d3e</item>  <!-- Genel arka plan rengi -->
    </style>

    <style name="Theme.SocialUni" parent="Base.Theme.MySocilUni" />
</resources>
